import 'package:flutter/material.dart';

class ColorConstant {
  static const primaryBackgroundColor = Colors.black;
  static const secondaryBackgroundColor = Color(0xFF1f1f1f);

  static const primaryTextColor = Colors.white;
  static const secondaryTextColor = Color(0xFFbbbbbb);

  static const favoriteDisableColor = Colors.white;
  static const favoriteEnableColor = Colors.red;

  static const buttonBackgroundColor = Colors.green;

  static const activeTabColor = Colors.green;
  static const inactiveTabColor = Colors.white;
}
